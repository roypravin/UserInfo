This is a Simple User Authentication Form. 
The fg_membersite.php upadte the database and take care validation.

--> login.php

The user can login through this login page. After successful login, the user is sent to the page login-home.php

--> register.php

This script displays the registration form.

Also provision of Password reset if User Forgot the Password. it will happen via sensing a password link. User can use that link and will update the onfo.
